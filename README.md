# next-intl-demo

with i18n routing

## run dev

git clone this repo

```
npm install
npm run dev
```

## pub to aliyun FC

```
npm run build
zip -rq -y code.zip ./
```

Login Aliyun FC, create a function, update the `code.zip` as function code.
